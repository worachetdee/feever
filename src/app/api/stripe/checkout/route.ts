import { NextResponse } from "next/server";

export async function POST() {
  // TODO: Create Stripe Checkout session with Connect transfer
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
